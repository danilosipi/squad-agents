# Copie para `.env` na mesma pasta (`repos/cap-base/`) se quiser que o Prisma carregue a URL automaticamente.
# O `prisma validate` precisa de uma URL sintaticamente válida (não é necessário o servidor estar acessível).
DATABASE_URL="postgresql://USER:PASSWORD@127.0.0.1:5432/cap_base?schema=public"
